export const enum TypesDemande {
    DEMANDE = "demande",
    OTHER = "other"
}

export const enum Roles {
    ADMIN = "admin",
    AGENT = "agent",
    USER = "user"
}

export const AccessTokenLocalStorage = 'access_token';
